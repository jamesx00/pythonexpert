---
layout: layouts/base.njk
eleventyNavigation:
  key: Home
  order: 1
---

<div class="px-6 pt-2">
  {% include 'settings.njk' %}
  <div class="max-w-4xl mx-auto py-10">
    <h2 class="text-xl md:text-2xl text-primary-700 mb-6">Welcome to PythonExpert! 🚀</h2>
    <p class="mb-4 text-gray-700">
      PythonExpert is a
      <strong>free</strong>
      and
      <strong>open-source</strong>
      web application built to help anyone learn how to program, step by step.
    </p>
    <p class="text-gray-700">
      👇 Check out all free courses below 👇
    </p>
  </div>
  <div class="max-w-4xl mx-auto py-10">
    <div class="grid md:grid-cols-3 gap-6">
      {% for course in collections.course|filter("hidden", undefined) %}
        <div class="relative group mb-12">
          <h4 class="mb-2">{{ course.data.title }}</h4>
          {% if course.data.is_under_development %}
            <div class="text-xs mb-4 bg-yellow-300 rounded-md px-2 inline-block">
              <span class="text-slate-600">Under development</span>
            </div>
          {% endif %}
          <p class="text-gray-600 mb-4">{{ course.data.description }}</p>
          <a href="{{ course.url }}" class="text-sm text-blue-600 hover:underline stretched-link">Start learning</a>
        </div>
      {% endfor %}
    </div>
  </div>
</div>