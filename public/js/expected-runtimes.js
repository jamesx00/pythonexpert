// Runtime versions the site's code editors are built against.
// Shared between lesson execution (lesson.js) and the runtime checker page.
const EXPECTED_RUNTIMES = {
	python: "3.10.0",
	javascript: "16.3.0",
	typescript: "4.2.3",
};
